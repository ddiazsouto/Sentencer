nginx:
  hosts:
    balancer:
      ansible_user: destruktor
      # ansible_ssh_private_key_file: "~/.ssh/id_rsa"
      # ansible_ssh_common_args: '-o StrictHostKeyChecking=no'

bosses:
  hosts:
    master:
    leader1:
    
employees:
  hosts:
    worker:
      ansible_user: destruktor
      ansible_ssh_private_key_file: "~/.ssh/id_rsa"
      ansible_ssh_common_args: '-o StrictHostKeyChecking=no'        
    
    # vars:
    #   ansible_user: jenkins
    #   ansible_ssh_private_key_file: "~/.ssh/ansible_id_rsa"
    #   ansible_ssh_common_args: '-o StrictHostKeyChecking=no'